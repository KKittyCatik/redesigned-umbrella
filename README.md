# PR Reviewer Assignment Service

![Go Version](https://img.shields.io/badge/Go-1.25+-blue.svg)

Сервис для автоматического назначения ревьюверов на pull requests с интеллектуальным распределением нагрузки между участниками команды.

## 📋 Требования

- Go 1.25+
- PostgreSQL 15+

## ⚙️ Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/KKittyCatik/redesigned-umbrella.git
cd redesigned-umbrella
```

### 2. Настройка окружения

```bash
cp .env.example .env
# Отредактируйте .env файл с вашими настройками
```

### 3. Запустите postgres

```bash
docker run --name pr-reviewer-db -e POSTGRES_PASSWORD=password \
  -e POSTGRES_DB=reviewer_service -p 5432:5432 -d postgres:15
```

### 4. Используйте команды MAkefile 

```bash
make build - сбильдить проект
make run - запустить проект
```
### 5. Миграции

### 6. Запуск через docker

```bash
docker compose up -d
```

## 🎯 API Endpoints
Команды

|Метод	     |Endpoint	    |Описание|
|-------------|-------------|-------------|
|POST	|/team/add|	Создать команду с участниками|
|GET	|/team/get|	Получить команду с участниками|

Пользователи

|Метод	|Endpoint|	Описание|
|-------------|-------------|-------------|
|POST	|/users/setIsActive|	Установить флаг активности пользователя|
|GET	|/users/getReview|	Получить PR'ы пользователя для ревью|

Pull Requests

|Метод	|Endpoint|	Описание|
|-------------|-------------|-------------|
|POST	|/pullRequest/create	|Создать PR и назначить ревьюверов|
|POST	|/pullRequest/merge	|Пометить PR как MERGED|
|POST	|/pullRequest/reassign	|Переназначить ревьювера|